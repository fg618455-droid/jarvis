"""Frozen measurements of behaviour that the subscription rebuild changes.

Each module here measures one property of the current system against a fixed
catalogue and writes the result to ``docs/baselines/``. The recorded numbers
are the reference the replacement is reported against, so the catalogues are
fixed: changing a question or a corpus entry invalidates the comparison.
"""
